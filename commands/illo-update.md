---
description: Update the illo plugin to the latest version and restart the daemon.
---

```bash
bash "${CLAUDE_PLUGIN_ROOT}/bin/illo-update.sh"
```

Report the output to the user verbatim. If the command fails (non-zero exit), say so clearly and suggest running it manually from a terminal so the user can see the full error.
